ingress-nginx:
  controller:
    service:
      type: LoadBalancer
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "10254"
    metrics:
      enabled: true
    podAnnotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "10254"

frontendIngress:
  host: ""
  serviceName: "frontend"
  servicePort: 80

appNamespace: "services"
